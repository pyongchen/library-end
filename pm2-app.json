{
  "apps": [{
    "name": "library",
    "script": "./src/server.js",
    "watch": true,
    "ignore_watch": ["*.log", ".git", "yarn.lock", "node_module", "static", "pm2-app.json", ".idea"],
    "force": true
  }]
}